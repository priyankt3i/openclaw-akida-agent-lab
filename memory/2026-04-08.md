## 2026-04-08 02:20:00 UTC
- Durable preference: The user wants more delegation wherever possible. Default to parallelizing work through specialized sub-agents when it improves throughput, focus, or turnaround.
- Durable project guidance: Favor Akida toolchain survival over elegant-but-incompatible attention forms. Prefer supported primitive surrogates, run early `cnn2snn.check_model_compatibility(...)` checks, defend against TENN on global-memory/context retention rather than short-range recurrence, use context-decay diagnostics as investor-facing evidence, and postpone hardware purchase until a 2-bit surrogate clears conversion and simulated power gates.
- Operating setup requested: Keep one sub-agent focused on dashboard improvements and another focused on the core technical Akida objective.
## 2026-04-08 02:20:00 UTC
- Durable preference: The user wants more delegation wherever possible. Default to parallelizing work through specialized sub-agents when it improves throughput, focus, or turnaround.
- Durable project guidance: Favor Akida toolchain survival over elegant-but-incompatible attention forms. Prefer supported primitive surrogates, run early `cnn2snn.check_model_compatibility(...)` checks, defend against TENN on global-memory/context retention rather than short-range recurrence, use context-decay diagnostics as investor-facing evidence, and postpone hardware purchase until a 2-bit surrogate clears conversion and simulated power gates.
- Operating setup requested: Keep one sub-agent focused on dashboard improvements and another focused on the core technical Akida objective.

## 2026-04-08 03:01:00 UTC
- Durable preference: The user expects dashboard work to be genuinely delegated, not just discussed. Keep an active dashboard-focused sub-agent when that workstream is in play, and be explicit when delegation is or is not actually running.
- Environment note: The user is sensitive to stale gateway/process clutter and forwarded ports. When ports appear, verify and explain concretely. Current confirmed listeners were one `openclaw-gateway` stack on `18789` (main gateway), `18791` (browser-control service), and `36309` (local browser CDP/debug), rather than three separate agent instances.
## 2026-04-08 02:20:00 UTC
- Durable preference: The user wants more delegation wherever possible. Default to parallelizing work through specialized sub-agents when it improves throughput, focus, or turnaround.
- Durable project guidance: Favor Akida toolchain survival over elegant-but-incompatible attention forms. Prefer supported primitive surrogates, run early `cnn2snn.check_model_compatibility(...)` checks, defend against TENN on global-memory/context retention rather than short-range recurrence, use context-decay diagnostics as investor-facing evidence, and postpone hardware purchase until a 2-bit surrogate clears conversion and simulated power gates.
- Operating setup requested: Keep one sub-agent focused on dashboard improvements and another focused on the core technical Akida objective.
## 2026-04-08 02:20:00 UTC
- Durable preference: The user wants more delegation wherever possible. Default to parallelizing work through specialized sub-agents when it improves throughput, focus, or turnaround.
- Durable project guidance: Favor Akida toolchain survival over elegant-but-incompatible attention forms. Prefer supported primitive surrogates, run early `cnn2snn.check_model_compatibility(...)` checks, defend against TENN on global-memory/context retention rather than short-range recurrence, use context-decay diagnostics as investor-facing evidence, and postpone hardware purchase until a 2-bit surrogate clears conversion and simulated power gates.
- Operating setup requested: Keep one sub-agent focused on dashboard improvements and another focused on the core technical Akida objective.

## 2026-04-08 03:01:00 UTC
- Durable preference: The user expects dashboard work to be genuinely delegated, not just discussed. Keep an active dashboard-focused sub-agent when that workstream is in play, and be explicit when delegation is or is not actually running.
- Environment note: The user is sensitive to stale gateway/process clutter and forwarded ports. When ports appear, verify and explain concretely. Current confirmed listeners were one `openclaw-gateway` stack on `18789` (main gateway), `18791` (browser-control service), and `36309` (local browser CDP/debug), rather than three separate agent instances.

## 2026-04-08 03:06:00 UTC
- Environment note: `prototype-1/dashboard.py` is currently a standalone manual process, not a supervised service. If its parent session ends, it will stop and will not auto-restart. When the user asks whether the dashboard is running, verify the live process/listener state rather than assuming the file being present means the dashboard is active.
## 2026-04-08 02:20:00 UTC
- Durable preference: The user wants more delegation wherever possible. Default to parallelizing work through specialized sub-agents when it improves throughput, focus, or turnaround.
- Durable project guidance: Favor Akida toolchain survival over elegant-but-incompatible attention forms. Prefer supported primitive surrogates, run early `cnn2snn.check_model_compatibility(...)` checks, defend against TENN on global-memory/context retention rather than short-range recurrence, use context-decay diagnostics as investor-facing evidence, and postpone hardware purchase until a 2-bit surrogate clears conversion and simulated power gates.
- Operating setup requested: Keep one sub-agent focused on dashboard improvements and another focused on the core technical Akida objective.
## 2026-04-08 02:20:00 UTC
- Durable preference: The user wants more delegation wherever possible. Default to parallelizing work through specialized sub-agents when it improves throughput, focus, or turnaround.
- Durable project guidance: Favor Akida toolchain survival over elegant-but-incompatible attention forms. Prefer supported primitive surrogates, run early `cnn2snn.check_model_compatibility(...)` checks, defend against TENN on global-memory/context retention rather than short-range recurrence, use context-decay diagnostics as investor-facing evidence, and postpone hardware purchase until a 2-bit surrogate clears conversion and simulated power gates.
- Operating setup requested: Keep one sub-agent focused on dashboard improvements and another focused on the core technical Akida objective.

## 2026-04-08 03:01:00 UTC
- Durable preference: The user expects dashboard work to be genuinely delegated, not just discussed. Keep an active dashboard-focused sub-agent when that workstream is in play, and be explicit when delegation is or is not actually running.
- Environment note: The user is sensitive to stale gateway/process clutter and forwarded ports. When ports appear, verify and explain concretely. Current confirmed listeners were one `openclaw-gateway` stack on `18789` (main gateway), `18791` (browser-control service), and `36309` (local browser CDP/debug), rather than three separate agent instances.
## 2026-04-08 02:20:00 UTC
- Durable preference: The user wants more delegation wherever possible. Default to parallelizing work through specialized sub-agents when it improves throughput, focus, or turnaround.
- Durable project guidance: Favor Akida toolchain survival over elegant-but-incompatible attention forms. Prefer supported primitive surrogates, run early `cnn2snn.check_model_compatibility(...)` checks, defend against TENN on global-memory/context retention rather than short-range recurrence, use context-decay diagnostics as investor-facing evidence, and postpone hardware purchase until a 2-bit surrogate clears conversion and simulated power gates.
- Operating setup requested: Keep one sub-agent focused on dashboard improvements and another focused on the core technical Akida objective.
## 2026-04-08 02:20:00 UTC
- Durable preference: The user wants more delegation wherever possible. Default to parallelizing work through specialized sub-agents when it improves throughput, focus, or turnaround.
- Durable project guidance: Favor Akida toolchain survival over elegant-but-incompatible attention forms. Prefer supported primitive surrogates, run early `cnn2snn.check_model_compatibility(...)` checks, defend against TENN on global-memory/context retention rather than short-range recurrence, use context-decay diagnostics as investor-facing evidence, and postpone hardware purchase until a 2-bit surrogate clears conversion and simulated power gates.
- Operating setup requested: Keep one sub-agent focused on dashboard improvements and another focused on the core technical Akida objective.

## 2026-04-08 03:01:00 UTC
- Durable preference: The user expects dashboard work to be genuinely delegated, not just discussed. Keep an active dashboard-focused sub-agent when that workstream is in play, and be explicit when delegation is or is not actually running.
- Environment note: The user is sensitive to stale gateway/process clutter and forwarded ports. When ports appear, verify and explain concretely. Current confirmed listeners were one `openclaw-gateway` stack on `18789` (main gateway), `18791` (browser-control service), and `36309` (local browser CDP/debug), rather than three separate agent instances.

## 2026-04-08 03:06:00 UTC
- Environment note: `prototype-1/dashboard.py` is currently a standalone manual process, not a supervised service. If its parent session ends, it will stop and will not auto-restart. When the user asks whether the dashboard is running, verify the live process/listener state rather than assuming the file being present means the dashboard is active.

## 2026-04-08 03:25:00 UTC
- Durable preference: Dashboard activity labels must be strict and truthful. Mark work as `in_progress` only when the main agent or a sub-agent is actively executing it. If nobody is actively working, mark milestones/tasks/phase state as `paused` rather than leaving them as active.
